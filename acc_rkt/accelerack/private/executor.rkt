#lang racket

;; Manage the Accelerack runtime.  Launch programs.

(provide launch-accelerack-ast)

(define (launch-accelerack-ast ast)
  (printf "TODO/FINISHME: Accelerack needs to launch this program: ~a\n" ast)
  )
